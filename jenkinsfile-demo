pipeline
{
    agent any
    stages
    {
        stage('copy sode from git url')
        {
            steps{sh'echo code downloded'}
        }
        stage('testing')
        {
            steps{sh'echo testing completed'}
        }
        stage('artifact created')
        {
            steps{sh'echo artifact created'}
        }
        stage('copy artifact to jfrog')
        {
            steps{sh'echo uploaded artifact to jfrog'}
        }
        stage('copy artifact to jfrog')
        {
            steps{sh'echo uploaded artifact to jfrog'}
        }
    }
}